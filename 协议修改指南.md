# 协议 JSON 修改指南

本文档教你：协议（硬件/上位机通讯协议）发生变化后，怎么动手改
`examples/sample_protocol.json` 这类协议配置文件，改完怎么验证没改错。
键的权威定义见 `软件需求说明.md` 第 2.3 节，本文档是"怎么落地改"的操作手册，
出现分歧以需求说明为准。

---

## 0. 先分清楚你是哪种情况

| 情况 | 说明 | 风险 |
|------|------|------|
| **A. 硬件协议没变，只是想改解析方式** | 比如给已有字段加个 `unit`、改个 `scale`、字段改名 | 低，随便试，解不出来大不了改回来 |
| **B. 硬件协议真的变了** | 设备固件升级、新增/删除了字段、校验算法换了、帧头变了 | **高，必须先拿到新版协议文档/新采集数据逐字节核对，不能凭空改** |

B 类改动如果只改 JSON 不核对真实字节，最常见后果是：帧长算错、同步字搜不到、
校验全部失败——而且这类错误 `decode2csv` 不会"报错"，只会安安静静地把
`垃圾字节数`/`校验失败丢弃帧数` 拉满、`成功帧数` 变 0，你需要自己看统计数字才能
发现协议配错了（见第 4 节"怎么验证"）。

---

## 1. 键值速查表

### 1.1 顶层

| 键 | 必填 | 说明 |
|----|------|------|
| `name` | 是 | 协议名，纯展示 |
| `description` | 否 | 说明文字，纯展示 |
| `endian` | 否，默认 `little` | 整体字节序，`little`/`big` |
| `frame` | 否 | 缺省 = 无同步字、无校验（定长顺序模式） |
| `fields` | 是 | 非空数组，按帧内先后顺序排列 |

顶层出现表外的键（比如打错成 `edian`）会直接拒绝加载，报错会指出具体是哪个键。

### 1.2 `frame`

| 键 | 必填 | 说明 |
|----|------|------|
| `sync` | 否 | 十六进制串（可含空格/大小写不敏感），偶数位，缺省=定长模式 |
| `checksum` | 否 | 缺省=不校验 |

### 1.3 `frame.checksum`

| 键 | 必填 | 取值 |
|----|------|------|
| `type` | 是 | `sum8` / `xor8` / `crc16-ccitt` / `crc16-modbus` |
| `range` | 否，默认 `frame` | `frame`（同步字开始到最后字段结束）/ `payload`（只算字段区，不含同步字） |
| `on_fail` | 否，默认 `drop` | `drop`（校验失败整帧丢弃）/ `keep`（校验失败仍输出，CSV 加"校验"列标 FAIL） |

### 1.4 `fields[]`

| 键 | 必填 | 说明 |
|----|------|------|
| `name` | 是 | 非空、协议内不重复，做 CSV 列名 |
| `type` | 是 | 见下表 |
| `size` | 仅 `bytes`/`char`/`padding` 必填 | 字节数；其他类型**不能**出现这个键 |
| `count` | 否，默认 1 | 数组长度；`padding` **不能**出现这个键 |
| `scale` | 否，默认 1 | 仅整型/浮点型可用；`bytes`/`char`/`padding` 不能出现 |
| `offset` | 否，默认 0 | 同上 |
| `unit` | 否 | 附加到 CSV 表头，如 `vel[0](m/s)` |
| `endian` | 否 | 覆盖整体字节序，仅本字段生效 |

### 1.5 字段类型表

| type | 字节数 | 说明 |
|------|--------|------|
| `uint8`/`int8` | 1 | |
| `uint16`/`int16` | 2 | |
| `uint32`/`int32` | 4 | |
| `uint64`/`int64` | 8 | |
| `float32` | 4 | IEEE754 单精度 |
| `float64` | 8 | IEEE754 双精度 |
| `bytes` | `size` | 输出大写 hex 串，如 `EB90FF` |
| `char` | `size` | 输出 ASCII 字符串；含不可打印字节则退化为 hex |
| `padding` | `size` | 跳过，不产生 CSV 列，但计入帧长和校验范围 |

**帧长自动计算**：`帧长 = sync 字节数 + Σ(字段字节数 × count) + 校验字节数`
（sum8/xor8 占 1 字节，crc16-* 占 2 字节）。这个不用你手动填，程序加载协议时
自动算出来，GUI 摘要和加载报错里都能看到实际算出的值——**改完协议第一件事就是
看这个数对不对**，见第 4 节。

---

## 2. 分场景操作步骤

### 2.1 新增一个字段

在 `fields` 数组里对应位置插入一条即可，注意**顺序必须和真实字节流顺序一致**
（字段是按数组顺序、无空隙地连续从字节流里切的，不是按 name 或别的规则匹配）。

```diff
   { "name": "nav_err_code",   "type": "uint8" },
+  { "name": "battery_mv",     "type": "uint16", "unit": "mV" },
   { "name": "vel", ...
```

加完之后帧长会自动变长（本例 +2 字节）。**如果同步字里编码了"数据长度"这种会
随字段变化的字节，同步字面值也要跟着改**——这是最容易漏改的一步，见 2.8 节详细展开。

### 2.2 删除一个字段

直接从 `fields` 数组里删掉那一条。如果这个字段中间的字节在新协议里确实不再
使用但仍然占位（设备只是不发这个字段了、但没有把后面字段往前挪），改成
`padding` 而不是直接删除：

```diff
-  { "name": "nav_err_code",   "type": "uint8" },
+  { "name": "_rsv_nav_err",   "type": "padding", "size": 1 },
```

真删除（字节流里也彻底没有这段了）才直接删数组项。

### 2.3 修改字段类型/长度

比如设备把某个字段从 `uint16` 升级成 `uint32` 携带更大量程：

```diff
-  { "name": "task_time", "type": "uint32", "unit": "ms" },
+  { "name": "task_time", "type": "uint64", "unit": "ms" },
```

注意 `size` 键**只属于** `bytes`/`char`/`padding`，数值类型改类型就是直接换
`type` 的值，不要给数值类型加 `size`（加了会报错拒绝加载）。

### 2.4 修改数组长度（count）

```diff
-  { "name": "effective_flg", "type": "uint8", "count": 3 },
+  { "name": "effective_flg", "type": "uint8", "count": 4 },
```

CSV 表头会自动从 `effective_flg[0..2]` 变成 `effective_flg[0..3]`，不用你手改
表头相关的任何东西。

### 2.5 修改整体字节序

```diff
-  "endian": "little",
+  "endian": "big",
```

如果只是**某一个字段**字节序特殊（比如大部分小端，个别寄存器大端），不要动
顶层 `endian`，给单个字段加 `endian` 覆盖：

```diff
   { "name": "legacy_reg", "type": "uint16", "endian": "big" },
```

### 2.6 修改校验算法 / 范围 / 失败策略

```diff
   "checksum": {
-    "type": "crc16-ccitt",
+    "type": "crc16-modbus",
     "range": "frame",
-    "on_fail": "drop"
+    "on_fail": "keep"
   }
```

改成 `on_fail: keep` 后，CSV 会多一列"校验"（值 OK/FAIL），方便你自己核对哪些
帧过去被悄悄丢弃了、丢弃的量大不大——**协议刚变更、还没摸清新格式时，建议先
临时改成 `keep` 观察，确认稳定后再按需要改回 `drop`**。

### 2.7 定长模式 ⇄ 同步字模式互相切换

- 定长转同步字：给 `frame` 加上 `sync` 键（十六进制串）。
- 同步字转定长：把 `frame.sync` 删掉（或整个 `frame` 键都删掉，连校验一起没了）。

定长模式下如果数据流中间丢字节/插字节，后面所有帧全部错位解析错误，且无法
自我恢复（详见 `README.md`"帧定位模式"一节的局限性说明）。**只要设备输出的
数据有可能出现丢字节/插字节的风险（比如通过串口/无线传输），都应该配置
`sync` 而不是用定长模式**，哪怕协议本身没有专门设计"同步字"这个概念——
只要帧头有几个字节是固定不变的（哪怕只是流水号之外的类型标识位），都可以
拿来当 `sync` 用。

### 2.8 【重要】同步字里编码了"数据长度"字段时的连锁修改

`examples/sample_protocol.json` 用的"计算机字"协议里，`frame.sync` 是
`"EB3E8500"`，对应 4 个字节：

```
EB          帧头，固定
3E          帧 ID，固定（标识"计算机字"这个帧型）
85 00       数据长度，小端 UINT16 = 0x0085 = 133（= 当前 fields 字节总数）
```

**这个"数据长度"不是随便定的常量，它就是当前 `fields` 字段区的字节总数**，
之所以能把它和帧头、帧 ID 一起塞进 `sync`，是因为对"计算机字"这一种帧型来说，
只要字段没变，这 4 个字节永远是同一个值，可以当成固定同步图样处理。

**一旦你改了 `fields`（加字段/删字段/变长度），这个"数据长度"字节的字面值
也会变，`sync` 必须跟着改，否则同步字搜索会因为字节不匹配而找不到任何帧**
（表现为解码结果 `成功帧数=0`、`垃圾字节数≈文件总字节数`）。

以 2.1 节新增 `battery_mv`（uint16，+2 字节）为例：

1. 新字段区总长度：133 + 2 = **135** = `0x0087`
2. 小端存储：`87 00`
3. 新 `sync`：`"EB3E8700"`（原来是 `"EB3E8500"`）

```diff
   "frame": {
-    "sync": "EB3E8500",
+    "sync": "EB3E8700",
     "checksum": { "type": "crc16-ccitt", "range": "frame", "on_fail": "drop" }
   },
```

**这一步的值不能靠自己拍脑袋算，必须用真实新数据核实**：拿一份设备升级后
新采集的 `.DAT` 文件，用 `xxd` 或类似工具看开头几个字节，确认第 3-4 字节
（"数据长度"位置）实际是不是 `87 00`——协议文档和真实字节对不上时，永远以
真实字节为准（文档可能没同步更新）。

> 这个"同步字里塞了变长字段"的设计是**这一种协议特有的**，不是所有协议都
>这样。如果你以后接入的是别的协议，`sync` 通常就是几个纯常量字节（帧头/
> 帧类型），跟字段数量无关，改字段不用改 `sync`。拿到新协议时先确认清楚
> `sync` 部分的字节是不是"纯常量"还是"常量里混了会变的字段"，这一点判断
> 错了会导致后面全错。

---

## 3. 完整改造示例：给"计算机字"协议加一个字段

场景：假设设备固件升级，在原有 `ecef_pz` 字段之后新增一个 `uint16` 的
`battery_mv`（电池电压，单位 mV，无需 scale），且这段"数据长度"字节按
2.8 节规则确实从 `0x0085` 变成了 `0x0087`（已用新采集的 `.DAT` 核实过）。

**改动前**（`examples/sample_protocol.json` 末尾片段）：

```json
    { "name": "ecef_px", "type": "int32", "scale": 0.001, "unit": "m" },
    { "name": "ecef_py", "type": "int32", "scale": 0.001, "unit": "m" },
    { "name": "ecef_pz", "type": "int32", "scale": 0.001, "unit": "m" }
  ]
}
```

**改动后**：

```json
  "frame": {
    "sync": "EB3E8700",
    "checksum": { "type": "crc16-ccitt", "range": "frame", "on_fail": "drop" }
  },
  "fields": [
    ...
    { "name": "ecef_px",     "type": "int32",  "scale": 0.001, "unit": "m" },
    { "name": "ecef_py",     "type": "int32",  "scale": 0.001, "unit": "m" },
    { "name": "ecef_pz",     "type": "int32",  "scale": 0.001, "unit": "m" },
    { "name": "battery_mv",  "type": "uint16", "unit": "mV" }
  ]
}
```

同步改一下开头的 `sync`（见上一节）。改完后：
- 字段区：133 + 2 = 135 字节
- 帧长：4（sync）+ 135 + 2（crc16）= **141 字节**（比原来的 139 多 2）

---

## 4. 改完必须做的验证（别跳过）

1. **看 GUI 摘要里的"帧长"是不是你手算的期望值。** 上面例子应该显示 141。
   算出来的数和你手算的对不上，说明字段数/size/count 哪里配错了，先别急着
   拿真数据去解码。
2. **如果协议改动涉及 `examples/sample_protocol.json`，`examples/gen_sample_data.py`
   和 `tests/test_decoder.py` 里写死的字段布局/校验向量要同步改**，否则
   `python3 -m pytest tests/ -v` 里 `SampleProtocolAcceptanceTests` 那几个
   用例会失败（这是好事——它就是用来在你忘记同步改测试时报警的）。
3. **用新协议 + 一段真实数据跑 CLI**，看输出统计：

   ```bash
   python3 main.py <新协议.json> <新数据.DAT> -o /tmp/check.csv
   ```

   重点看这一行：

   ```
   文件总字节数=... 成功帧数=... 校验失败丢弃帧数=... 垃圾字节数=... 尾部残余字节数=...
   ```

   - `成功帧数` 接近 `文件总字节数 / 新帧长`、`垃圾字节数` 接近 0 —— 协议基本配对了。
   - `成功帧数=0` 或 `垃圾字节数` 占了文件的大部分 —— 协议没配对（最常见是
     2.8 节说的 `sync` 没跟着字段变化改，或者字段顺序/类型/size 和真实字节流
     对不上）。
4. **打开生成的 CSV，挑几个你知道真实物理含义的字段人工核对数值**（比如
   电压应该在合理范围、时间戳应该递增），确认不是"凑巧解出来但内容全错"。

---

## 5. 协议加载报错怎么读

协议 JSON 有问题时程序会**直接拒绝加载**（GUI 弹窗 / CLI 打到 stderr 并非
0 退出），报错都是中文，格式固定为"错在哪个字段 + 哪个键 + 期望什么 + 实际是什么"，例如：

下面每一条都是**从实际运行的代码里跑出来的原文**（不是凭印象编的示例）：

| 报错 | 含义 | 怎么改 |
|------|------|--------|
| `协议错误：fields[1]（名称 "b"） 的 "size" 必须为正整数，实际为 "abc"` | 第 2 个字段（从 0 计数）的 `size` 写错了 | 改成正整数 |
| `协议错误：fields[0]（名称 "a"） 存在未知键：sacle` | 键名拼错了（`scale` 拼成 `sacle`），这类拼写错误会被直接拒绝，不会静默忽略 | 改成正确键名 |
| `协议错误：fields[0]（名称 "a"） 的类型 "bytes" 不允许出现 "scale"` | 给 `bytes`/`char`/`padding` 类型加了 `scale`/`offset` | 这几个类型不支持换算，删掉这个键 |
| `协议错误："frame.sync" 必须为偶数个十六进制字符且至少 1 字节，实际为 "ABC"` | `sync` 写了奇数个 hex 字符 | 补齐成偶数位，比如 `ABC` 改成 `0ABC` 或 `ABC0`（具体补哪边看真实字节） |
| `协议错误：fields[1]（名称 "a"） 的字段名与之前字段重复` | 两个字段用了同一个 `name` | 改成不重复的名字 |
| `协议错误：JSON 语法错误（第 12 行）：...` | JSON 本身格式错了（少逗号/少引号） | 定位到给出的行号修 |

这类报错会**拒绝加载**、不会解码出任何数据，属于"改协议改错了程序直接告诉你"
的情况，相对好排查。真正麻烦的是第 4 节里"协议加载成功但解出来的数据是错的/
是空的"这种，务必按第 4 节流程验证。
