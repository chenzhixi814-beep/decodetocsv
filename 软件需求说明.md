# 二进制数据解码工具（decode2csv）软件需求说明

- **版本**：v1.1
- **日期**：2026-07-09
- **实现语言**：Python 3（≥3.10）
- **交付形式**：源码 + 单文件可执行程序（PyInstaller 打包，可在无 Python 环境的电脑上运行）
- **参考协议**：仓库内《通讯协议文档.pdf》（帧头 EB + 帧ID + 数据长度 + 数据 + CRC16）。
  本文档已将其关键信息（帧结构、CRC 算法、示例帧型）完整转录，实现时**以本文档为准**，无需再读 PDF。

> **给实现者（AI agent）的说明**：本文档是唯一权威依据。所有"必须/不得/默认"均为硬性要求；
> 第 5 节（帧定位与异常处理）和第 6 节（校验算法）给出了精确到字节的行为定义与测试向量，
> 实现与测试都必须逐条对照。遇到本文档未定义的情况，选择"不崩溃、报告、跳过"的保守行为。

---

## 1. 项目背景与目标

在数据采集、遥测、嵌入式通信等场景中，原始数据通常以二进制帧的形式记录到文件中。
本软件的目标是提供一个**通用的、由配置文件驱动的**二进制解码工具：用户通过图形界面选择
"协议配置文件"和"原始数据文件"，软件按协议描述将二进制数据逐帧解析，并输出为 CSV 文件，
便于用 Excel、Python、MATLAB 等工具做后续分析。

核心设计原则：

1. **协议与程序分离**：新增或修改协议只需编写/修改一个 JSON 配置文件，无需改动程序代码。
2. **解析正确性优先**：功能保持简单，但对每一种输入（含非法输入）的行为都有确定性定义，
   宁可少解出帧，也不得输出错误数据或崩溃。

## 2. 功能需求

### 2.1 交互界面（FR-1）

| 编号 | 需求描述 |
|------|----------|
| FR-1.1 | 提供图形用户界面（GUI），基于 Python 内置 tkinter 实现，无额外运行时依赖 |
| FR-1.2 | 可通过文件选择对话框选择**协议配置文件**（`.json`） |
| FR-1.3 | 可通过文件选择对话框选择**原始数据文件**（任意二进制文件，支持一次选择多个，逐个解码） |
| FR-1.4 | 可指定输出 CSV 文件的保存目录；默认与数据文件同目录同名（扩展名改为 `.csv`）。输出文件已存在时直接覆盖 |
| FR-1.5 | 选择协议文件后，界面显示协议摘要：协议名、字节序、**自动计算的帧长**、字段列表（名称/类型/数组长度/单位）。协议文件非法时在界面上显示中文错误并拒绝开始解码 |
| FR-1.6 | 提供"开始解码"按钮；解码在后台线程执行，通过 `queue` + `after()` 回传进度（按已处理字节数/文件总字节数）与日志，主界面不卡死；解码期间"开始"按钮置灰 |
| FR-1.7 | 每个文件解码完成后在日志区输出统计（见 5.6），全部完成后弹窗汇总（成功帧数、丢弃/标记帧数、垃圾字节数、输出路径） |
| FR-1.8 | 界面与全部报错文案为中文 |

### 2.2 解码功能（FR-2）

| 编号 | 需求描述 |
|------|----------|
| FR-2.1 | 按选定协议将原始数据文件解码为 CSV 文件，编码 `utf-8-sig`（带 BOM，Excel 直接打开不乱码），行分隔用 csv 模块默认（`newline=''` 打开文件） |
| FR-2.2 | CSV 第一行为表头，其后每行对应一帧。表头格式见 5.5 |
| FR-2.3 | 支持字段类型与长度见下表（4.2 节） |
| FR-2.4 | 支持整体字节序配置（`little`/`big`，默认 `little`），单字段可用 `endian` 键覆盖 |
| FR-2.5 | 支持数组字段（`count > 1`）：连续读取 count 个同类型值，输出为 `名称[0]…名称[count-1]` 多列 |
| FR-2.6 | 支持线性工程量换算：`物理值 = 原始值 × scale + offset`（对应协议文档中的"数据分辨率 LSB"，接收方乘以 LSB 即 `scale = LSB`） |
| FR-2.7 | 支持两种帧定位方式：① 无 `frame.sync`：定长顺序切分；② 有 `frame.sync`：同步字搜索，容忍任意位置的垃圾字节。精确算法见第 5 节 |
| FR-2.8 | 支持可选校验：`sum8`/`xor8`/`crc16-ccitt`/`crc16-modbus`，精确定义与测试向量见第 6 节；校验失败行为由 `on_fail` 决定（见 5.3） |
| FR-2.9 | 文件尾部不足一帧的残余数据丢弃并计入统计 |
| FR-2.10 | 流式解码：按块读取（建议块大小 `max(1 MiB, 帧长×2)`），跨块保留残余缓冲；**禁止一次性读入整个文件**，内存占用与文件大小无关 |
| FR-2.11 | 命令行模式：`decode2csv <协议.json> <数据文件>... [-o 输出.csv]`。无参数启动 GUI；`-o` 仅在单个数据文件时允许，多文件时输出各自同名 `.csv`。解码结束将统计打印到 stdout；致命错误（协议非法/文件打不开/输出不可写）打印中文错误到 stderr 并以非 0 退出码结束，正常结束（含 0 帧）退出码为 0，0 帧时打印警告 |

### 2.3 协议配置文件（FR-3）

协议配置文件为 JSON（UTF-8，允许带 BOM）。完整示例见附录 B。骨架：

```json
{
  "name": "协议名",
  "description": "说明（可选）",
  "endian": "little",
  "frame": {
    "sync": "EB3E8500",
    "checksum": { "type": "crc16-ccitt", "range": "frame", "on_fail": "drop" }
  },
  "fields": [
    { "name": "counter", "type": "uint16" },
    { "name": "vel",     "type": "int32", "count": 3, "scale": 0.001, "unit": "m/s" },
    { "name": "tag",     "type": "char",  "size": 4 },
    { "name": "_rsv",    "type": "padding", "size": 2 }
  ]
}
```

#### 键定义（含默认值）

| 键 | 必填 | 取值/默认 | 说明 |
|----|------|-----------|------|
| `name` | 是 | 非空字符串 | 协议名，显示在界面上 |
| `description` | 否 | 字符串 | 仅展示 |
| `endian` | 否 | `"little"`（默认）/ `"big"` | 整体字节序 |
| `frame` | 否 | 对象 | 缺省 = 无同步字、无校验 |
| `frame.sync` | 否 | 十六进制串 | 同步字（可含帧头/帧ID/定长长度字段等**所有固定不变的前导字节**）。偶数个 hex 字符（忽略大小写与空格），≥1 字节。缺省则定长顺序切分 |
| `frame.checksum` | 否 | 对象 | 缺省 = 不校验 |
| `frame.checksum.type` | 是* | `sum8` / `xor8` / `crc16-ccitt` / `crc16-modbus` | *checksum 对象存在时必填 |
| `frame.checksum.range` | 否 | `"frame"`（默认）/ `"payload"` | 校验覆盖范围：`frame` = 从同步字第一个字节到最后一个字段的最后一个字节（含 padding）；`payload` = 仅字段区（不含同步字） |
| `frame.checksum.on_fail` | 否 | `"drop"`（默认）/ `"keep"` | 校验失败处理，见 5.3 |
| `fields` | 是 | 非空数组 | 按帧内先后顺序排列 |
| `fields[].name` | 是 | 非空字符串，同协议内不得重复 | CSV 列名 |
| `fields[].type` | 是 | 见 4.2 表 | 字段类型 |
| `fields[].size` | 视类型 | 整数 ≥1 | `bytes`/`char`/`padding` 必填（字节数）；其他类型**不得出现** |
| `fields[].count` | 否 | 整数 ≥1，默认 1 | 数组长度；`padding` 类型不得出现 |
| `fields[].scale` | 否 | 数值，默认 1 | 仅整型/浮点型允许 |
| `fields[].offset` | 否 | 数值，默认 0 | 仅整型/浮点型允许 |
| `fields[].unit` | 否 | 字符串 | 附加到表头 |
| `fields[].endian` | 否 | `"little"` / `"big"` | 覆盖整体字节序；仅多字节整型/浮点型有意义 |

**严格校验**：顶层、`frame`、`checksum`、每个字段对象中出现上表之外的键一律报错
（防止 `sacle` 之类的拼写错误静默失效）。

**帧长自动计算**：`帧长 = sync 字节数 + Σ(字段字节数 × count) + 校验字节数`
（sum8/xor8 为 1 字节，crc16 为 2 字节）。协议加载后帧长必须 ≥1，同步字模式下
校验值紧跟在最后一个字段之后。CRC16 校验值本身按协议字节序存储读取
（《通讯协议文档.pdf》为小端）。

**协议加载错误报告**（NFR-3 的细化）：加载/校验失败时给出中文错误，必须包含：
出错位置（如 `fields[3]`，从 0 计数）、字段名（若有）、出错的键名、期望值与实际值。
例：`协议错误：fields[2]（名称 "tag"）的 "size" 必须为正整数，实际为 "abc"`。
JSON 语法错误时报告行号。

## 3. 非功能需求

| 编号 | 需求描述 |
|------|----------|
| NFR-1 | **可移植性**：PyInstaller 打包单文件可执行程序；Windows → `.exe`，Linux → ELF；目标机器无需 Python |
| NFR-2 | **性能**：解码速度不低于 20 MB/s（常规 PC）；GUI 后台线程解码不卡界面。建议用 `struct.Struct` 预编译整帧格式、按块批量搜索同步字 |
| NFR-3 | **健壮性**：任何非法输入（协议文件、数据文件、输出路径）都不得导致未捕获异常/崩溃，均给出中文提示；数据内容异常按第 5 节定义处理 |
| NFR-4 | **依赖**：运行时仅 Python 标准库（tkinter/struct/csv/json/threading/queue 等）；PyInstaller 仅为打包期依赖 |
| NFR-5 | **可维护性**：解码核心（protocol.py/decoder.py）不得 import tkinter，与 GUI 完全解耦，可脱离显示环境运行测试 |

## 4. 数据类型与值输出规则

### 4.1 位与字节约定（转录自《通讯协议文档.pdf》1.1/1.2）

字节内 bit0 为最低位、bit7 为最高位。多字节数据默认小端（低字节在前）。
有符号整数为补码。浮点为 IEEE 754。

### 4.2 字段类型表

| type | 字节数 | struct 码 | CSV 输出 |
|------|--------|-----------|----------|
| `uint8` / `int8` | 1 | `B` / `b` | 十进制整数（无 scale/offset 时） |
| `uint16` / `int16` | 2 | `H` / `h` | 同上 |
| `uint32` / `int32` | 4 | `I` / `i` | 同上 |
| `uint64` / `int64` | 8 | `Q` / `q` | 同上 |
| `float32` | 4 | `f` | 浮点，见 4.3 |
| `float64` | 8 | `d` | 浮点，见 4.3 |
| `bytes` | size | `{size}s` | 大写十六进制串，无分隔符（如 `EB90FF`） |
| `char` | size | `{size}s` | 字符串，见 4.3 |
| `padding` | size | 跳过 | **不输出列**，但计入帧长与校验覆盖范围 |

### 4.3 值转换与格式化（必须严格遵守，保证可测试）

1. **整型无 scale/offset**：输出十进制整数原值，不做任何转换。
2. **配置了 scale 或 offset 的整型/浮点**：`物理值 = float(原始值) * scale + offset`，
   输出 Python `repr(float)`（最短往返表示，如 `1.012`），不做四舍五入。
3. **float32/float64**：按 `repr()` 输出；遇 NaN/±Inf 分别输出 `nan` / `inf` / `-inf`，不报错。
4. **bytes[N]**：原始 N 字节 → 大写 hex 串。
5. **char[N]**：去掉**尾部**的 `\x00` 后按 ASCII 解码；若剩余字节中存在不可打印字符
   （不在 0x20–0x7E 范围内），则整个字段退化为大写 hex 串输出（与 bytes 相同），
   保证 CSV 永不因数据内容损坏或抛异常。
6. **校验标记列**（仅 `on_fail: "keep"` 时存在）：列名 `校验`，值 `OK` / `FAIL`。

## 5. 帧定位算法与异常处理（核心正确性规范）

本节定义对**任意字节流**（包括完全不符合协议的数据）的确定性行为。

### 5.1 定长顺序模式（无 `frame.sync`）

从文件第 0 字节起按帧长连续切分。若配置了校验则逐帧校验（失败处理见 5.3，
但**定长模式下 drop 只丢弃该帧、指针仍前进整帧长**，不做逐字节重同步——无同步字
就无从重同步）。尾部不足一帧的字节丢弃并计入 `尾部残余字节数`。

> 局限性说明（须写入 README）：定长模式对文件中间插入/丢失字节没有恢复能力，
> 错位后其余帧全部解错；这类数据应配置同步字。

### 5.2 同步字搜索模式（有 `frame.sync`）

维护一个字节缓冲区与扫描指针，循环执行：

1. 在缓冲区中查找 sync（`bytes.find`）。找不到：缓冲区中除末尾 `len(sync) - 1`
   字节外全部计入 `垃圾字节数` 并丢弃，读入下一块后继续（跨块拼接由此保证）。
2. sync 之前的字节计入 `垃圾字节数`。
3. 自 sync 起若不足一整帧：保留残余，读下一块补齐；已到文件尾则残余计入
   `尾部残余字节数`，结束。
4. 取出一整帧候选，按第 6 节计算校验：
   - **校验通过**（或未配置校验）：解出一行 CSV，指针前进整帧长。
   - **校验失败且 `on_fail: "drop"`**：视为**伪同步**（sync 图样恰好出现在垃圾或
     其他帧型的数据中），指针仅前进 **1 字节**，回到第 1 步重新搜索。
     该规则保证：真帧起始位置永远不会被跳过，垃圾中偶现 sync 图样不会吞掉真帧。
   - **校验失败且 `on_fail: "keep"`**：输出该行并在 `校验` 列标 `FAIL`，指针前进整帧长。

### 5.3 校验失败语义汇总

| 模式 | on_fail=drop | on_fail=keep |
|------|--------------|--------------|
| 同步字搜索 | 不输出；前进 1 字节重新搜索；计入 `校验失败丢弃帧数` | 输出并标 `FAIL`；前进整帧；计入 `校验失败保留帧数` |
| 定长顺序 | 不输出；前进整帧；计入 `校验失败丢弃帧数` | 同上 |

### 5.4 异常场景矩阵（实现与测试逐条对照）

| 编号 | 场景 | 要求的行为 |
|------|------|-----------|
| E-01 | 数据文件为空（0 字节） | 输出仅含表头的 CSV，统计 0 帧，日志警告"未解出任何帧"，不报错 |
| E-02 | 文件小于一帧 | 同 E-01，字节计入垃圾/残余 |
| E-03 | 文件头部/帧间有任意垃圾字节（同步模式） | 全部有效帧仍被解出，垃圾字节计数准确 |
| E-04 | 垃圾字节中恰好包含 sync 图样（伪同步） | 配置校验时：按 5.2 规则前进 1 字节，不丢真帧；未配置校验时按正常帧解出（无法区分，属协议配置责任，README 说明） |
| E-05 | 数据流中混有其他帧ID/其他类型的帧 | 非本协议 sync 的字节按垃圾跳过；若其他帧型前 4 字节与 sync 相同但内容不同，由校验拒绝（同 E-04） |
| E-06 | 文件尾部被截断（最后一帧不完整） | 残余丢弃并计入统计，之前的帧正常输出 |
| E-07 | 帧内浮点字段为 NaN/Inf 位型 | 按 4.3 输出，不报错 |
| E-08 | char 字段含非 ASCII/不可打印字节 | 按 4.3 退化为 hex 输出，不报错 |
| E-09 | 协议 JSON 语法错误 / 键非法 / 类型未知 / 缺 size / 重名字段 / 未知键 | 拒绝加载，中文报错并定位（见 2.3），GUI 不允许开始解码 |
| E-10 | 数据文件不存在/无读权限 | 中文报错，退出码非 0（CLI）/ 弹窗（GUI），不崩溃 |
| E-11 | 输出路径不可写/磁盘满 | 中文报错，已写部分文件状态不做保证但程序不崩溃 |
| E-12 | 协议帧长 > 数据块大小 | 块大小取 `max(1 MiB, 帧长×2)`，逻辑仍正确 |
| E-13 | 解码过程中用户关闭 GUI 窗口 | 后台线程以协作方式尽快终止，不留僵尸线程、不抛异常到控制台 |

### 5.5 CSV 表头规则

- 普通字段：`name`；有 unit 时：`name(unit)`，如 `vel[0](m/s)`。
- 数组字段：`name[0]` … `name[count-1]`（unit 追加在下标之后）。
- `padding` 不产生列；`on_fail: "keep"` 时最后追加 `校验` 列。
- 列顺序与协议 `fields` 顺序一致。

### 5.6 统计项（GUI 汇总、CLI stdout、日志共用）

`文件总字节数`、`成功帧数`（校验通过或未配置校验）、`校验失败丢弃帧数`、
`校验失败保留帧数`、`垃圾字节数`、`尾部残余字节数`。
不变式：`成功帧数×帧长 + 保留帧数×帧长 + 垃圾字节数 + 尾部残余字节数 + 丢弃重试消耗字节数 = 文件总字节数`
（drop 模式每次失败前进 1 字节，这些字节最终计入垃圾字节数）。

## 6. 校验算法精确定义（附测试向量）

校验计算对象是 `range` 指定的**原始字节区间**（与字段语义无关，padding 也在内）。
校验值存储位置：最后一个字段之后；16 位校验值按协议字节序存取。

| type | 定义 | `b"123456789"` 的结果 |
|------|------|----------------------|
| `sum8` | 所有字节累加和的低 8 位 | `0xDD` |
| `xor8` | 所有字节异或 | `0x31` |
| `crc16-ccitt` | CRC-16/XMODEM：多项式 0x1021，初值 **0x0000**，输入/输出不反转，无最终异或。**即《通讯协议文档.pdf》附录 A 的算法** | `0x31C3` |
| `crc16-modbus` | 多项式 0x8005（反射 0xA001），初值 0xFFFF，输入/输出反转，无最终异或 | `0x4B37` |

附加测试向量（来自《通讯协议文档.pdf》附录 A.2，必须写入单元测试）：
`crc16-ccitt(b"abcdefg1234567HIJKLMN") == 0xEEF7`。

## 7. 软件结构

```
decodetocsv/
├── 软件需求说明.md          # 本文档（权威依据）
├── 通讯协议文档.pdf          # 目标协议原始文档（已转录进本文档，实现时无需再读）
├── README.md                # 使用与打包说明（含 5.1 定长模式局限性说明）
├── main.py                  # 入口：无参数启动 GUI，带参数走 CLI（argparse）
├── decode2csv/
│   ├── __init__.py          # __version__
│   ├── protocol.py          # 协议 JSON 加载与校验 → Protocol 对象（帧长、struct 格式、校验函数）
│   ├── decoder.py           # 流式解码：bytes → 帧 → CSV 行（不 import tkinter）
│   └── gui.py               # tkinter 界面，调用 decoder
├── examples/
│   ├── sample_protocol.json # 附录 B 的"计算机字"协议
│   └── gen_sample_data.py   # 按《通讯协议文档.pdf》生成测试数据（见第 9 节）
├── tests/
│   └── test_decoder.py      # 覆盖 5.4 全部场景 + 第 6 节测试向量
└── build.sh / build.bat     # PyInstaller 一键打包脚本
```

## 8. 打包与发布

- `pyinstaller --onefile --windowed --name decode2csv main.py`
- PyInstaller 不支持交叉编译：`.exe` 须在 Windows 上打包。仓库同时提供
  `build.bat`（Windows）与 `build.sh`（Linux）。
- 发布物：可执行文件 + `examples/`。

## 9. 验收标准

1. **单元测试全绿**，且覆盖：第 6 节全部测试向量（含 PDF 附录 A.2 向量 `0xEEF7`）、
   5.4 异常矩阵 E-01～E-08、drop 伪同步场景（在垃圾区人为埋入 `EB3E8500` 图样，
   有效帧一帧不丢）。
2. `examples/gen_sample_data.py` 按附录 B 协议生成若干"计算机字"帧（含已知物理值经
   LSB 换算取整后写入、正确 CRC），并可选注入头部/帧间垃圾字节与一个坏 CRC 帧；
   用 sample_protocol.json 解码后，CSV 数值与生成时的期望物理值一致
   （整型列逐位相等，scale 列按 4.3 规则比对），坏帧按 on_fail 配置被丢弃/标记。
3. 双击可执行文件出中文 GUI，能加载协议显示摘要（帧长应显示 139），解码上述数据成功。
4. CLI 模式对同一数据产出与 GUI 相同的 CSV（逐字节一致）。
5. 协议 JSON 中人为制造 2.3 所列每类错误，均得到含定位信息的中文报错，程序不崩溃。

---

## 附录 A：帧结构（转录自《通讯协议文档.pdf》1.4）

| 序号 | 名称 | 起始 | 字节数 | 类型 | 说明 |
|------|------|------|--------|------|------|
| 1 | 帧头 | 0 | 1 | UINT8 | 固定 `0xEB` |
| 2 | 帧ID | 1 | 1 | UINT8 | 区分帧型（计算机字为 `0x3E`） |
| 3 | 数据长度 | 2 | 2 | UINT16 | 仅数据部分的长度 N（计算机字为 `0x0085`=133，小端存储为 `85 00`） |
| 4 | 数据部分 | 4 | N | — | 字段区 |
| 5 | 数据校验 | N+4 | 2 | UINT16 | 第 1～4 项（帧头到数据部分末尾）的 CRC16（第 6 节 `crc16-ccitt`），小端存储 |

由于"帧头+帧ID+数据长度"对固定帧型是 4 个常量字节，本工具将其整体作为
`frame.sync = "EB3E8500"`，配 `checksum.range = "frame"` 即可精确覆盖第 1～4 项。
计算机字帧总长 = 4 + 133 + 2 = **139 字节**。

## 附录 B：示例协议 sample_protocol.json（计算机字帧，转录自 PDF 表 7）

```json
{
  "name": "计算机字（帧ID 0x3E）",
  "description": "转录自《通讯协议文档.pdf》1.6.1 表7；LSB 即 scale",
  "endian": "little",
  "frame": {
    "sync": "EB3E8500",
    "checksum": { "type": "crc16-ccitt", "range": "frame", "on_fail": "drop" }
  },
  "fields": [
    { "name": "frame_seq",      "type": "uint8" },
    { "name": "utc_year",       "type": "uint16" },
    { "name": "utc_month",      "type": "uint8" },
    { "name": "utc_day",        "type": "uint8" },
    { "name": "utc_hour",       "type": "uint8" },
    { "name": "utc_minute",     "type": "uint8" },
    { "name": "utc_second",     "type": "uint32", "scale": 0.001, "unit": "s" },
    { "name": "task_time",      "type": "uint32", "unit": "ms" },
    { "name": "pulse_task_time","type": "uint32", "unit": "ms" },
    { "name": "gnss_fix_flag",  "type": "uint8" },
    { "name": "effective_flg",  "type": "uint8",  "count": 3 },
    { "name": "common_star_num","type": "uint8" },
    { "name": "nav_err_code",   "type": "uint8" },
    { "name": "vel",            "type": "int32", "count": 3, "scale": 0.001, "unit": "m/s" },
    { "name": "pos",            "type": "int32", "count": 3, "scale": 0.001, "unit": "m" },
    { "name": "dP_Bas",         "type": "int32", "count": 3, "scale": 0.001, "unit": "m" },
    { "name": "dV_Bas",         "type": "int32", "count": 3, "scale": 0.001, "unit": "m/s" },
    { "name": "dP_User",        "type": "int32", "count": 3, "scale": 0.001, "unit": "m" },
    { "name": "dV_User",        "type": "int32", "count": 3, "scale": 0.001, "unit": "m/s" },
    { "name": "dP_Relative",    "type": "int32", "count": 3, "scale": 0.001, "unit": "m" },
    { "name": "dV_Relative",    "type": "int32", "count": 3, "scale": 0.001, "unit": "m/s" },
    { "name": "ecef_px",        "type": "int32", "scale": 0.001, "unit": "m" },
    { "name": "ecef_py",        "type": "int32", "scale": 0.001, "unit": "m" },
    { "name": "ecef_pz",        "type": "int32", "scale": 0.001, "unit": "m" }
  ]
}
```

字段区合计：1+2+1+1+1+1+4 +4+4 +1+3+1+1 +27×4 = **133 字节**，与"数据长度"0x0085 一致；
帧长 = 4（sync）+ 133 + 2（CRC16）= **139 字节**（GUI 摘要应显示此值，可作自检）。

发送方按 LSB 缩小并**向下取整**后发送（PDF 1.6），因此生成测试数据时应模拟
`int(math.floor(物理值 / LSB))` 写入，解码端乘回 LSB。
